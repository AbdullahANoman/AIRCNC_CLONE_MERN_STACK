import React from 'react';

const CategoryBox = ({label,icon}) => {
    console.log(icon)
    return (
        <div>
            <p>{label}</p>
            
        </div>
    );
};

export default CategoryBox;